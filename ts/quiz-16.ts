const techStack: string[] = ["MongoDB", "Express", "React", "Node.js"];
// Destructuring
const [db, backend, frontend, server] = techStack;

console.log(db); // MongoDB
console.log(backend); // Express
console.log(frontend); // React
console.log(server); // Node.js